1. Исправить проблемы если не указываешь нечего а нажимаешь 
    интер програма выпадает в ошибку
2. Исправить проблему если в поля ответа попадает не чило тоже выпадает\
    в ошибку 
3. Научится чтобы из одного файла данные попадали в другой
    из .py в .html для вывода данных и затем проверялся
    ответ и давал реальный результат